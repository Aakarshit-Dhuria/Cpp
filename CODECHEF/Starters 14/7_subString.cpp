#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        string s1, s2, x;
        cin >> s1 >> s2 >> x;
        int count = 0;
        unordered_set<string> s;
        for()
    }
    
    return 0;
}