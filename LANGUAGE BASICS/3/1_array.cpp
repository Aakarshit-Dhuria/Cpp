#include<iostream>
using namespace std;

int main()
{
    int n;
    // cin >> n;
    int arr1[n];
    //! we can also have size of array as a variable
    int arr[] = {50, 30, 40, 20, 60};
    int arr2[]{10, 2, 3, 42, 4};  //! Alternate way to initialise an array.
    
    return 0;
}